package ozon

import (
	"context"
)

func (s *OzonAPI) GetSupply(ctx context.Context, sku string) error {
	// resp, err := s.client.Supply().GetSupply(ctx, &ozon.GetSupplyParams{
	// 	Sku: sku,
	// })
	return nil
}
